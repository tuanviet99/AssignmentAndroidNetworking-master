<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.androidnetworking.assandroidnetworking">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <uses-permission android:name="android.permission.SET_WALLPAPER_HINTS" />
    <uses-permission
        android:name="android.permission.BIND_WALLPAPER"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".GetContentActivity"></activity>
        <activity
            android:name=".FavoritesActivity"
            android:label="@string/favorites_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ImageDetailActivity"
            android:label="@string/imageDetail_name" />
        <activity
            android:name=".AboutUsActivity"
            android:label="@string/aboutus_name" /> <!-- android:parentActivityName=".LatestActivity" -->
        <activity
            android:name=".CategoryActivity"
            android:label="@string/category_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LatestActivity"
            android:label="@string/latest_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/home_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".SplashActivity"></activity>
    </application>

</manifest>